<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.CountryMapper">


        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
        country_code, name_cn, name_en, start_zip, end_zip, alias_name, type, home_country
    </sql>

    <!-- 查找详情 2020-09-04-->
    <select id="getById" resultType="com.apl.lms.common.pojo.po.vo.CountryInfoVo">
        SELECT
          country_code, 
          name_cn, 
          name_en, 
          start_zip, 
          end_zip, 
          alias_name, 
          type, 
          home_country
        FROM
          country
        where
          country_code = #{id}
    </select>

    <!-- 查找列表 2020-09-04-->
    <select id="getList" resultType="com.apl.lms.common.pojo.po.vo.CountryListVo">
        SELECT
           country_code, 
           name_cn, 
           name_en, 
           start_zip, 
           end_zip, 
           alias_name, 
           type, 
           home_country
        FROM
           country
        where
        1 = 1
        order by id desc
    </select>


</mapper>
