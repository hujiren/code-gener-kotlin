package ${package.Service};
import com.apl.common.utils.ResultUtils;

import ${package.Entity}.${entity}Po;
import ${cfg.vo}.${entity}Vo;
import ${cfg.dto}.${entity}Dto;
import ${superServiceClassPackage};
import com.apl.common.pojo.dto.PageDto;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
/**
 * <p>
 * $!{table.comment} 服务类
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
#if(${kotlin})
interface ${table.serviceName} : ${superServiceClass}<${entity}Po>
#else
public interface ${table.serviceName} extends ${superServiceClass}<${entity}Po> {

        /**
         * @Desc: 添加一个${entity}Po实体
         * @Author: CY
         * @Date: 2019/12/2 9:39
         */
        ResultUtils<Boolean> add(${entity}Po ${table.entityPath});

        /**
         * @Desc: 根据id 查找一个${entity}Po 实体
         * @Author: CY
         * @Date: 2019/12/2 9:39
         */
        ResultUtils<Boolean> deleteById(Long id);

        /**
         * @Desc: 根据id 更新一个${entity}Po 实体
         * @Author: CY
         * @Date: 2019/12/2 9:39
         */
        ResultUtils<Boolean> updById(${entity}Po ${table.entityPath});

        /**
         * @Desc: 根据id 查找一个 ${entity}Po 实体
         * @Author: CY
         * @Date: 2019/12/2 9:40
         */
        ResultUtils<${entity}Po> selectById(Long id);

        /**
         * @Desc: 分页查找 ${entity}Po 列表
         * @Author: CY
         * @Date: 2019/12/2 9:40
         */
        ResultUtils<Page<${entity}Vo>>getList(PageDto pageDto, ${entity}Dto keyDto);

        }
#end
