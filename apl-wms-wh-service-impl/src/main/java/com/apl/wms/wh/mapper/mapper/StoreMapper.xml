<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apl.wms.wh.mapper.StoreMapper">


    <!-- 查找详情 2019-12-11-->
    <select id="getById" resultType="com.apl.wms.wh.vo.StoreInfoVo">
        SELECT
        id, 
        store_code, 
        store_name, 
        store_name_en, 
        cust_id, 
        electric_code, 
        store_status, 
        api_config, 
        inner_org_id
        FROM
        store
        where
            id = #{id}
    </select>

    <!-- 查找列表 2019-12-11-->
    <select id="getList" resultType="com.apl.wms.wh.vo.StoreListVo">
        SELECT
        id, 
        store_code, 
        store_name, 
        store_name_en, 
        cust_id, 
        electric_code, 
        store_status, 
        api_config, 
        inner_org_id
        FROM
        store
        where
        1 = 1
    </select>

</mapper>
